[project]
name = "spyplanner"
version = "0.1.0"
description = "Daily plan generator for SPY (plan-only)."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
